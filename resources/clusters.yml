resources:
  clusters:
    job_cluster:
      spark_version: 14.3.x-scala2.12
      node_type_id: Standard_D4s_v3
      num_workers: 2
      init_scripts:
        - dbfs:
            destination: "dbfs:/scripts/install-wheel.sh"
